# Simplified requirements - avoids compilation issues
# Web Framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Web Scraping  
requests==2.31.0
beautifulsoup4==4.12.2
# selenium==4.15.2  # Skip for now - can add later if needed

# Database
sqlalchemy==2.0.23

# Data Processing
pandas==2.1.3
# numpy  # Will be installed as pandas dependency

# Environment and Configuration
python-dotenv==1.0.0
pydantic-settings==2.1.0

# CORS and Security
python-multipart==0.0.6

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Logging
loguru==0.7.2

# Basic text processing (instead of heavy NLP)
textblob==0.17.1

# Note: Heavy NLP packages (spacy, transformers) skipped to avoid build issues
# Can be installed later with proper build tools if needed 